# MEMORY — example agent

> This is the agent's persistent, human-readable memory. It carries forward
> across sessions and tick restarts. Keep it short (≤100 lines). The
> structured/vector memory lives in `memory.db`; this file is for high-level
> notes you want the agent to *always* see at the top of every prompt.

## What I know about my operator

(populate as the agent learns)

## Standing goals

(populate)

## Lessons that stuck

(populate — or use `aie attempt` to log structured ones into the DB)
